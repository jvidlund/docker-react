language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t jvidlund/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jvidlund/docker-react npm run test -- --coverage